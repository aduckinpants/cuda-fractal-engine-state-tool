# Question-Driven Research Session — Post-Run Contract Alignment

## Source evidence

The bounded refinement follows the complete paid run at:

```text
D:\salt-fractal\cuda-fractal-engine-state-tool\question-runs\question-research-6440724c-b38d-43de-9ec4-5af4720831a3
```

That run remains immutable evidence. It passed the unattended transaction,
spent `$0.0927388`, executed one five-member sweep, produced five replay-proven
members, retained the captured research base, sealed `ANSWER_PARTIAL`, cleaned
all provider files, and closed its event/projection authority coherently.

## Accepted refinements

### Observation censoring

Review now emits one exact classification for every single result or sweep
member:

```text
SUPPORTED
CONTRADICTED
CENSORED_OUT_OF_FRAME
UNOBSERVABLE
EXECUTION_FAILED
```

The controller validates exact result coverage and execution-status
consistency. Synthesis must copy the same outcomes from the hash-bound review
decision. A subject or measurement boundary outside the retained viewport is
censored and is neither confirmation nor contradiction.

### Cost contract

The live implementation keeps:

```text
review:     200,000 input / 8,000 output
synthesis:  100,000 input / 12,000 output
```

These are intentional amendments. Earlier preserved live attempts proved the
original 4,000-token review and 8,000-token synthesis ceilings too narrow. The
successful alignment source run used 2,417 review output tokens and 6,370
synthesis output tokens, but the lower historical limits are not reinstated.

### Scientific-record contract

The implemented claim-oriented V1 shape is retained and explicitly accepted.
It adds required structured:

```json
{
  "confidence_and_limitations": {
    "confidence": "LOW | MODERATE | HIGH",
    "limitations": ["at least one explicit limitation"]
  }
}
```

Experiment summaries also carry exact review-owned observation outcomes.
Current research-base and packet-lineage identity remain controller facts in
the synthesis context, active projection, and closeout instead of model-authored
scientific claims.

### Identity and lifecycle naming

New run evidence distinguishes:

```text
round_plan_contract_sha256
round_plan_canonicalization_version
round_plan_file_sha256

provider_request_dispatched
durable_response_recovered

working_session_status
alternate_communication_status
```

The ambiguous `round_plan_sha256`, misleading `provider_redispatch`, and
redundant generic `communication_status` names are not emitted by new runs.

## Local qualification

Focused protocol, controller, context, synthesis, result, provider, cost, and
runner courts cover:

- exact observation-outcome parsing and classifications;
- missing-member rejection;
- a replay-proven censored sweep member;
- synthesis equality with a referenced review decision;
- explicit canonical/file plan hashes;
- provider dispatch versus durable recovery;
- current cost ceilings;
- absence of the generic communication alias.

The complete Python 3.14 suite passed after the implementation change.

## Remaining qualification boundary

The prior paid run predates these wire contracts. One fresh count-gated epsilon
golden rerun is required to demonstrate that the live reviewer classifies an
out-of-frame endpoint as censored and that synthesis preserves the exact
classification plus structured confidence. It must remain under the already
approved `$0.30` hard run cap and stop again for user review. Broader automated
use remains unauthorized.
