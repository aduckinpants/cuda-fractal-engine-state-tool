# Packet V7 Compact Handoff — Manual Gate

## Status

Implementation and local proof are complete. Three fresh, previously unused
captures are prepared for user-run web sessions. This document is the stop
boundary; the implementation agent does not simulate or self-grade those
sessions.

All packets bind:

- runtime identity SHA-256:
  `140707fd16283ac2db3c58a24146b73a7f29bbd15b0e1b36771c301f8b275e95`
- descriptive catalog SHA-256:
  `8038ab867cd40dd4af6ca5b26aca11cd5e7c6b6a28816b00f7d4afdb4a4909fd`
- seven required uploads: six authority/context files plus one bounded PNG
- no recommended extra uploads

Paste `packet.md`; do not upload it as an eighth file. Then upload exactly the
seven filenames shown by the packet and desktop checklist:

```text
state.json
fractal-state.json
fractal-viewport-facts.json
state-authoring-authorities.md
color-pipeline-authority.md
finding-context.md
web-agent-frame.png
```

The packet directory still contains the individual JSON authorities and exact
source frame. They remain local proof/validation evidence and are intentionally
not part of the compact web upload set.

## H1 — ExplainO Halley

- Purpose: ordinary dynamics-authoring and no-pipeline transport case
- Source:
  `D:\salt-fractal\cuda_newton_fractal_clone\findings\manual_capture\2026-07-19\090748_064__explaino_halley`
- Finding ID:
  `547d06fe491163c8c37858dbce5c0fdca313beddeb2f257a906ce6b5c644a49c`
- Packet ID: `98c3ccf6-f99b-4125-abd0-437751527342`
- Packet directory:
  `D:\salt-fractal\cuda-fractal-engine-state-tool\findings\547d06fe491163c8c37858dbce5c0fdca313beddeb2f257a906ce6b5c644a49c\packets\98c3ccf6-f99b-4125-abd0-437751527342`
- Packet SHA-256:
  `fd5df06dfa1b0de4df1bca34f8492f41601fbb74c614fd07f20c80266431be4d`
- Manifest SHA-256:
  `de1ea2f67f00cdf6301b5f6850b3858707172ed042b56d299849bd5383d23b84`
- Selected description status: `reviewed`
- Required upload bytes: `753644`
- Source frame: `4096 × 2560`, SHA-256
  `46455b37ae521c5a7b5f558b5984637af2031e8906a27ec7cd4b29f259155893`
- Web PNG: `2048 × 1280`, SHA-256
  `b1b02b8ca16c77b3bb66134e737f613bc23a5eac24b480881f20cb08fb356b94`

Targeted prompt:

```text
Choose one mathematically motivated, state-authorable Halley dynamics experiment rather than a color-only change. Identify the observation channel and decide whether the current camera is an honest same-window comparison, feature-tracking view, or transition survey.
```

This packet has no complete captured Color Pipeline draft. The session should
not infer that the embedded full UI-Salt contract makes Color Pipeline override
authoring available.

## D1 — ExplainO Dual Seed

- Purpose: paired-seed semantics and viewport-continuity reasoning
- Source:
  `D:\salt-fractal\cuda_newton_fractal_clone\findings\manual_capture\2026-07-19\092648_441__explaino_dual`
- Finding ID:
  `4f7b251270d243abbeb123167a4d1ecc6b7edf879d6766f36b74a08120aea66a`
- Packet ID: `05375cdc-e277-42f2-8f97-535339837d2b`
- Packet directory:
  `D:\salt-fractal\cuda-fractal-engine-state-tool\findings\4f7b251270d243abbeb123167a4d1ecc6b7edf879d6766f36b74a08120aea66a\packets\05375cdc-e277-42f2-8f97-535339837d2b`
- Packet SHA-256:
  `8fb052750b14ffeb1be8db598c8343431a8a0ed93b28e07d13d6507dacca6391`
- Manifest SHA-256:
  `4bca098b028680a6b2ec4d81bf87f44984d2a702a7dc58cef2fa1bc6b21215c8`
- Selected description status: `reviewed`
- Required upload bytes: `1598800`
- Source frame: `4096 × 2560`, SHA-256
  `7f60abbe72dc05c64c256149f8fd718d916fc7aad8ed2eb339bcc61f6dfcf083`
- Web PNG: `2048 × 1280`, SHA-256
  `fc309477b10a75fff7436fa36489809ee277322ba34d1f0b0962880092e44f32`

Targeted prompt:

```text
Choose one state-authorable experiment involving the Dual Seed construction rather than color alone. Distinguish whether it changes the primary seed, secondary seed, blend, or more than one of those, and account for the current viewport honestly.
```

The session should select one state, not compress a sweep or several seed/blend
alternatives into one override.

## P1 — ExplainO Phoenix

- Purpose: complete nontrivial Color Pipeline transport, high-zoom dynamics,
  and large-image reduction
- Source:
  `D:\salt-fractal\cuda_newton_fractal_clone\findings\manual_capture\2026-07-16\094450_410__explaino_phoenix`
- Finding ID:
  `941afc4c9bd8787c0a670a18c1d488f394373ad444f3d6dafe6c66c13efe5225`
- Packet ID: `2da8f77b-2138-446f-ade3-1b7cf9adb0a4`
- Packet directory:
  `D:\salt-fractal\cuda-fractal-engine-state-tool\findings\941afc4c9bd8787c0a670a18c1d488f394373ad444f3d6dafe6c66c13efe5225\packets\2da8f77b-2138-446f-ade3-1b7cf9adb0a4`
- Packet SHA-256:
  `84382edfe306c7c17222b1bfa01b18d5690a6afc539f93c14365d3ba380dafcd`
- Manifest SHA-256:
  `e12d97cb9f0f402628a529b69e98d010cd8434a11c065a8d87965b4ef2ddd541`
- Selected description status: `reviewed`
- Required upload bytes: `4255561`
- Source frame: `4096 × 2560`, about `16.98 MiB`, SHA-256
  `700b35faf299c141ce2708b7b3dbd1806a8d1bc3b3ecf21f45d4ce73804217b2`
- Web PNG: `2048 × 1280`, about `3.57 MiB`, SHA-256
  `07553525f9876fd281006e6915841fe6c233a314cbf5bc7f9fa39e1699b56218`

Targeted prompt:

```text
Choose one mathematically motivated Phoenix-memory dynamics experiment rather than color alone. This is an extreme-zoom capture: decide honestly whether the current subject can be retained, tracked, or only surveyed, and do not infer small visual impact from a small coefficient change.
```

This packet has a complete pipeline draft and embeds the exact structural
example and complete UI-Salt contract in `color-pipeline-authority.md`. The
session may discuss color, but the targeted experiment is a dynamics and camera
stress test.

## Common prompt sequence

Opening:

```text
What do you notice here? What seems mathematically interesting or worth exploring?
```

Follow-up:

```text
What would you try?
```

Then use the fixture's targeted prompt. It explicitly delegates selection of one
experiment and may itself trigger a valid preflight and JSON. If the response
already contains one valid preflight and one JSON block, do not add a redundant
trigger. Otherwise, after exactly one state has been selected:

```text
Let's do that. Return the exact sparse state override for this finding.
```

Before rendering:

```text
Before I render this, describe in plain language what you expect to see. Name two to four observable visual or numerical signs and the strongest result that would contradict your interpretation. This is discussion only; do not revise or repeat the override.
```

After returning the follow-up capture:

```text
Compare this capture with your prediction. Separate what matched, what missed, and what remains uncertain. This is discussion only; do not return another override unless I explicitly request a revision.
```

## Acceptance checklist

- The web client retains all seven filenames and exposes each file's content.
- The agent distinguishes the exact state/viewport files from consolidated
  transport views.
- The agent does not claim the consolidated Markdown files are replacement
  schemas or new authority.
- The agent recognizes `web-agent-frame.png` as a 2048-by-1280 bounded
  discussion derivative.
- Any exact pixel/color statistic is explicitly scoped to the derivative; it is
  not presented as a full-resolution capture result.
- Exploration precedes override output.
- No override appears before one concrete experiment is selected.
- An ambiguous selection produces one clarification question, not `{}`.
- A returned override is nonempty unless unchanged-state replay was explicitly
  requested.
- Every changed leaf is present in the embedded finding-specific authoring
  surface.
- Camera reasoning uses the exact separately attached viewport facts.
- The desktop tool loads the exact pre-existing packet directory; do not click
  `Refresh Bundle`.
- Proof reaches `VISUAL REVIEW PENDING` before acceptance.
- `Open Full Candidate` opens
  `materialization\candidate-display.png`, while the receipt proves its decoded
  RGBA identity equals `materialization\frame.bmp`.

## Local implementation proof

- Focused bundle, override, proof, preview, workflow, and UI tests passed.
- Full Python 3.14 suite: 86 passed.
- Real H1 scalar proof:
  `33c5381f-3c2d-419d-8f5c-d19b38f8e40e`
- The H1 proof changed `params.explaino_damping` from `1.0` to `0.99`, reached
  replay-proven/visual-review-pending, and emitted a full-resolution PNG whose
  decoded RGBA SHA-256 equals the authoritative BMP:
  `28aa48ec82d720066de28bba2b4c00e09f149e669f7761cb97b83b049b03721b`.
- Real desktop capture loaded the exact existing H1 Packet V7, rendered base
  and candidate previews, and stopped with launch disabled at visual review
  pending. Raw evidence:
  `.local/compact_handoff_hardening/ui/`.

## Stop boundary

The user performs these three web sessions and returns transcripts, tool
validation/proof outcomes, and follow-up captures. A real transport ambiguity
may reopen this bounded campaign. Model noncompliance alone does not justify
recreating the former giant packet or adding another surrogate authority.
