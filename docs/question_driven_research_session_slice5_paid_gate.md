# Question-Driven Research Session — Slice 5 Offline Qualification and Paid Gate

## Gate status

```text
offline implementation qualification: PASS
fresh Packet V8 authority: PASS
golden epsilon sweep: PASS
provider count-only request: PASS — exact first request counted under user-approved $0.30 cap
first provider generation attempt: CLOSED — protocol correction exhausted before experiment execution
golden rerun: pending hardened literal-header and durable-response recovery checkpoint
```

The user authorized one bounded golden generation under a `$0.30` hard cap.
The first live attempt selected a valid epsilon sweep but omitted the mandatory
colon in the planner action header twice. It closed without executing an
experiment or sealing a scientific conclusion. The exact run evidence and
hardening response are recorded in
`docs/question_driven_research_session_live_golden.md`.

## Fresh golden authority

The published runtime had changed after the prior UI-only Packet was created,
so Slice 5 generated a new coherent Packet V8 from the approved source capture.

```text
source capture:
D:\salt-fractal\cuda_newton_fractal_clone\findings\manual_capture\2026-08-05\143014_930__explaino_transcendental

packet:
D:\salt-fractal\cuda-fractal-engine-state-tool\findings\3209208bc5ccbc09a8f36fbc60b95d125f082761e0195b8c9aaee1af80f3c2cc\packets\efa80582-f798-4380-aa60-578a8f0e2e40

packet ID:
efa80582-f798-4380-aa60-578a8f0e2e40

manifest SHA-256:
a2fb815a94c9ce104d1378863f98b30e46d654fc004f10925a121668938a197a

runtime executable SHA-256:
26db530a4e73aa482f5752ed6bc35e7e5092dd94277aa417be25668d338c7560
```

The packet exposes `params.epsilon` as an applicable, present, direct float
state path with range `1e-12` through `0.01`. Its captured value is the
engine-emitted float representation `9.9999999747524271e-07`.

The exact visible observation chain is:

```text
Phase Orbit [phase_orbit]
-> Identity [identity]
-> Phase Wheel [phase_wheel_palette]
-> Phase Finish [phase_finish]
```

## Golden local experiment proof

Tracked inputs:

- `docs/manual-test-results/question-research-golden/epsilon-research-brief.json`
- `docs/manual-test-results/question-research-golden/epsilon-sweep-plan.json`

The checked-in brief has a deliberate `$0.00` hard budget and authorizes only
`params.epsilon`. The local Scalar Bracket Sweep V1 preflight generated five
independent concrete overrides from the fresh packet. The published engine then
materialized and action-free replayed all five:

```text
5e-7       REPLAY_PROVEN
7.5e-7     REPLAY_PROVEN
1.25e-6    REPLAY_PROVEN
1.5e-6     REPLAY_PROVEN
2e-6       REPLAY_PROVEN
```

Evidence:

```text
C:\code\cuda-fractal-engine-state-tool\.local\slice5-question-research-sweeps\b3cd802b-d188-437a-8e13-d7fff9bb9022
```

The contact sheet includes the captured base as a labeled observation tile and
shows a monotonic visible expansion across the bracket. This is local
experiment evidence only; no scientific conclusion has been synthesized by a
provider and no human acceptance is recorded.

## Offline controller courts

The focused court covers:

- all four planner actions and every unresolved-reason taxonomy value;
- locked prediction and payload identity;
- exact path/domain permissions and no-op experiment rejection;
- one correction and two-attempt ceilings;
- single override, scalar sweep, member failure, promotion, and rebinding;
- exact cost counting contracts, mandatory review/synthesis reserves, and
  conservative adaptive ceilings;
- budget refusal before experiment execution, deterministic
  `BUDGET_EXHAUSTED` closeout, and no provider retry;
- fresh review/synthesis contexts and active Color Pipeline inclusion;
- rooted/hash-bound evidence resolution;
- invalid synthesis fallback without fabricated claims;
- alternate communication failure without invalidating sealed science;
- provider-file cleanup;
- append-only events, atomic projections, crash recovery, and Windows
  `PermissionError` / WinError 5 and 32 replacement contention;
- scalar-sweep plan/member failure and cancellation behavior;
- five-question UI form, sanitized event presentation, and exact count-gate
  invalidation.

Focused court:

```text
88 passed
```

Complete Python 3.14 suite after the final implementation pass:

```text
281 passed
```

## Cost disclosure before count-only

Tracked pricing authority:

```text
policy: openai-standard-2026-08-03
policy SHA-256: 7934d36c4865098494c52da4001f6c008b92a8f0cce2e261b8bd273ed44c088e
model profile: gpt-5.6-luna / high / standard / explicit_no_cache
profile SHA-256: dbf174b6e03c074b26588e606e6ddb4a27eb5d36bcbb70184863116e39e268b7
```

Local policy-derived worst-case ceilings are:

```text
one experiment attempt + correction + synthesis:  $0.1688
two experiment attempts + correction + synthesis: $0.2632
two attempts + required alternate communication:  $0.2804
```

These are deterministic stage-cap ceilings, not predicted invoices. The exact
first planner input token count and exact first-call estimate remain unknown
until the provider count endpoint processes the fully prepared request.

## Exact manual count gate

1. Launch:

   ```powershell
   .\run_ui.cmd `
     --capture-source "D:\salt-fractal\cuda-fractal-engine-state-tool\findings\3209208bc5ccbc09a8f36fbc60b95d125f082761e0195b8c9aaee1af80f3c2cc\packets\efa80582-f798-4380-aa60-578a8f0e2e40" `
     --workspace-root "D:\salt-fractal\cuda-fractal-engine-state-tool"
   ```

2. Open `Research Question…`.
3. Enter the tracked golden brief. Keep Luna/high and `working_session`.
4. Enter the hard dollar budget the user is willing to approve.
5. Press `Count & Review Budget`.
6. Review the displayed:

   - exact first planner input-token count;
   - exact estimated first-call maximum;
   - required cost including review, synthesis, and correction reserves;
   - conservative adaptive ceiling;
   - hard budget and authorization result.

7. Report those values for explicit generation approval. Do not press
   `Run Research` yet.

The count approval is bound to the exact Packet, brief, model, reasoning, and
budget. The generation run records the exact source count-run manifest and
authorization hashes in `cost/preflight-count-gate.json`. Any edit forces a
recount.

## Hostile closure review

- No historical packet was treated as current after runtime drift.
- The golden path was proven through the canonical sweep/proof owner, not a
  research-only executor.
- The checked-in brief cannot spend money.
- A run cannot bypass or reuse a stale count gate.
- Budget exhaustion is a controller closeout, not an unresolved-reason value
  the model selects.
- A budget refusal consumes no experiment attempt and receives no automatic
  retry.
- The UI and provider contexts show the exact active Color Pipeline rather than
  requiring inference from raw state.
- Provider generation remains uncalled and unauthorized.

Slice 5 is complete through the approved offline boundary. The only remaining
planned campaign work is the separately authorized count-only preflight and,
after review of its exact numbers, one separately authorized golden generation
run followed by user review.
