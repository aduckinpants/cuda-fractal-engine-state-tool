# Question Research Golden Court

## Current exact authority

Source capture:

```text
D:\salt-fractal\cuda_newton_fractal_clone\findings\manual_capture\2026-08-05\143014_930__explaino_transcendental
```

Fresh Packet V8 generated during Slice 5:

```text
D:\salt-fractal\cuda-fractal-engine-state-tool\findings\3209208bc5ccbc09a8f36fbc60b95d125f082761e0195b8c9aaee1af80f3c2cc\packets\efa80582-f798-4380-aa60-578a8f0e2e40
```

```text
packet ID: efa80582-f798-4380-aa60-578a8f0e2e40
manifest SHA-256: a2fb815a94c9ce104d1378863f98b30e46d654fc004f10925a121668938a197a
runtime executable SHA-256: 26db530a4e73aa482f5752ed6bc35e7e5092dd94277aa417be25668d338c7560
```

The packet is external immutable evidence. The tracked brief and sweep plan are
the human-reviewable golden inputs. The brief intentionally retains a `$0.00`
hard budget, so it cannot authorize provider generation as checked in.

## Qualified scope

Only `params.epsilon` is executable. The five-value Scalar Bracket Sweep V1
plan excludes the exact captured base value and keeps all other state paths
fixed. The active observation chain is:

```text
Phase Orbit [phase_orbit]
-> Identity [identity]
-> Phase Wheel [phase_wheel_palette]
-> Phase Finish [phase_finish]
```

The research court asks for the narrowest evidence-supported relationship, not
an exact proportional law. A truthful unresolved result is valid.

## Paid boundary

No provider count or generation call is authorized by this directory. At the
manual gate:

1. open the fresh Packet V8 in the application;
2. open `Research Question…`;
3. enter the tracked brief (or equivalent exact fields);
4. enter an explicitly approved hard dollar budget;
5. press `Count & Review Budget`;
6. review the exact initial count, estimated first call, required reserves, and
   conservative adaptive ceiling;
7. separately approve generation before pressing `Run Research`.

Any Packet, brief, model, reasoning, or budget edit invalidates the count gate.
